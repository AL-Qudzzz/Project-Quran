<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add Internet Permission if API access is needed -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectGrup6"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- MainActivity (launcher activity) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MenuActivity -->
        <activity android:name=".MenuActivity"></activity>

        <!-- ReadQuranActivity -->
        <activity android:name=".ReadQuranActivity"></activity>

        <!-- AyatActivity -->
        <activity android:name=".AyatActivity"></activity>

        <!-- AdhanActivity -->
        <activity android:name=".AdhanActivity"></activity>

        <!-- SelectLocationActivity -->
        <activity android:name=".SelectLocationActivity"></activity>

    </application>

</manifest>
